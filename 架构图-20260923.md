# 04 · 当前架构图（2026-09-23 状态）

> 对应委托方问④："写出目前架构图。"
> 依据：`ARCHITECTURE-定版v1.1-20260923.md`（两轴）+ `ARCHITECTURE-定版v1.0`（冻结清单）+ 五份清单实况 + 本次复跑读数。
> 记法：**文字图（ASCII）+ 四张表（两轴/三产品/一码四包/验证状态）**；所有数字为本次实跑或 S7 在案读数。

---

## 一 · 总图

```
                    ┌────────────────────────────────────────────┐
                    │  治理位（人）· 在库外 · 不入任何形态          │
                    │  验收 · 裁决 · 签 cap · 打 tag               │
                    │  —— 执行无权自宣验收（冻结第4条）            │
                    └───────────────────┬────────────────────────┘
                                        │ 令 / 验收 / 裁决
   ┌────────────────────────────────────┴─────────────────────────────────────┐
   │                     产品面 = 等级 × 装配（三产品）                          │
   │                                                                           │
   │   ┌──────────────────┐     ┌──────────────────┐     ┌──────────────────┐  │
   │   │ 微内核            │     │ 中库              │     │ 共库              │  │
   │   │ L1 × 融合单文件   │     │ L1 × 包内自足     │     │ L2 × 服务件       │  │
   │   │ evo-seat 0.2.2    │     │ kb/ 规范 1.0.0    │     │ MCP server 0.1.0  │  │
   │   │ 六档 G0-G5        │     │ make_kb.py 构建   │     │ 八工具 + caps     │  │
   │   │ (cap 豁免:静态档位)│    │ 可插拔 gov/       │     │ (cap 四关强制)    │  │
   │   └────────┬─────────┘     └────────┬─────────┘     └────────┬─────────┘  │
   └────────────┼────────────────────────┼────────────────────────┼────────────┘
                └───────────┬────────────┴─────────────┬──────────┘
                            │     一码四包 · 装配链      │
                  ┌─────────▼───────────────────────────▼─────────┐
                  │  build/assemble.py                            │
                  │  五清单登记（fused/gov/evo/pypkg/server）      │
                  │  reconcile 五查 · check-copy 两级对账          │
                  └─────────┬───────────────────────────┬─────────┘
                            │                           │
              ┌─────────────▼───────────┐  ┌────────────▼────────────────────────┐
              │ 一码（三个核）            │  │ 四包产物（build/out/）                │
              │ · audit-kit 1.0.0 治理核 │  │ · fused/    融合单文件                │
              │ · evocore   0.3.0 演化核 │  │ · vendored/ gov/ + evo/ 包内自足      │
              │ · evo-seat  0.2.2 融合核 │  │ · pypkg/    pip 依赖                  │
              └─────────────┬───────────┘  │ · server/   服务件 + MCP 壳           │
                            │              └─────────────────────────────────────┘
              ┌─────────────▼───────────┐
              │ 宿主与消费面              │
              │ · memsys（记忆宿主）      │
              │ · blender skill 三副本    │
              │ · dist/发布-sys-v1.0.zip  │
              └─────────────────────────┘
```

## 二 · 两轴（v1.1 修宪定版）

| 轴 | 定义 | 值域 |
|---|---|---|
| **等级轴** | 信任边界 | **L1**（自己 vs AI 单个）· **L2**（自己 vs 自己的 Agent vs 别人的 Agent）· L3（组织间，插座期） |
| **装配轴** | 一码四包 | 融合单文件 · 包内自足 · pip 依赖 · 服务件 |

## 三 · 三产品（= 等级 × 装配的组合）

| 产品 | 组合 | 状态 | 治理机制 |
|---|---|---|---|
| **微内核** | L1 × 融合单文件 | ✅ evo-seat **0.2.2** | 六档审查精细度 **G0-G5**（能力令牌=**豁免声明**，SPEC §A） |
| **中库** | L1 × 包内自足 | ✅ kb 规范 **1.0.0**（S3 交付） | 可插拔 gov/（vendored + framework_sha 对账） |
| **共库** | L2 × 服务件 | ✅ **L2-0** MCP server **0.1.0**（S4 交付；L2-1…L2-4 未启动） | **能力令牌四关**（NO_CAP/SCOPE/CANNOT/EXPIRED）+ caps 骨架 |
| ~~L1 多库~~ | L1 × pip 依赖 | 不占产品名（**降格为开发/自用装配**） | — |

**pip 形态不占独立产品名**（v1.1 §2：打包差异不构成等级）。

## 四 · 一码四包（装配与登记）

| 环节 | 件 | 状态 |
|---|---|---|
| 装配器 | `build/assemble.py`（all/单个；conformance 联动） | ✅ `assemble all` = 4/4 产物过 conformance |
| 中库构建器 | `build/make_kb.py`（init/migrate/check/audit；迁移三同） | ✅ `check` = 29/30+N/A |
| 服务件 | `build/l2server/server.py`（八工具；stdio+HTTP；单写者） | ✅ selftest 9/9 · 客户端 9/9 |
| 发布打包 | `build/make_release.py`（确定性 zip + verify + smoke） | ✅ 双跑逐位 · 自校验 PASS |
| 登记 | `build/manifests/` 五份（逐件 sha + aggregate） | ✅ `reconcile` 五查 PASS |
| 副本对账 | `check-copy` 两级 + `framework_sha` | ✅ 融合核机制锚 `a98f559753d7cbe0` |

## 五 · 验证状态（本次复跑）

| 形态 | 读数 |
|---|---|
| 融合 | **14/14 PASS** |
| 治理核（重装） | **9/9 PASS** |
| 治理核+宿主 | **12/12 PASS** |
| 中库 | **29/30 PASS（N/A 1——未检查，非通过）** |
| 服务件 | selftest **9/9** · 客户端 **9/9** |
| 套件总账（S7 在案） | evo 76 · ak 49 · mem 17 · kb 8 · server 10 · assemble 8 · demo 6 · docs 6 = **180**，全 OK |
| 端到端演示 | 跨形态差异 **0 项** |

> **运维注记（本次盘点实操发现）**：`reconcile` 五查 PASS 针对**出厂态**产物；服务件被真实使用（`ops grant` 授权 / MCP 客户端会话）后，部署目录会落地运行时状态（`caps.json` · `libs/*.db` · `ops/calls.jsonl`——**不在产物清单内**），对账将报 DRIFT——**不是缺陷**，重建即还原（`py -X utf8 build/assemble.py all`；本次盘点已实操：重建后五查 PASS）。

## 六 · 冻结清单七条（v1.1 修订版 · 改动走修宪）

1. 两轴定义（等级轴=信任边界；装配轴=一码四包）
2. 一码四包装配原则
3. SPEC §D 五硬条（跨形态语义）
4. **治理位在库外**（不问等级不问形态）
5. 中库三轴裁定：规模降/部署降/**纪律不降**
6. **微内核不掺和 L2**（独立性是功能）
7. 形态对应关系（由产品名映射表承载）

## 七 · 未交付面（图上没有的）

| 面 | 状态 |
|---|---|
| **治理操作面**（S8：中库六命令/revoke/override 通道/prereg 生效/来源视图最小版/说明书治理节） | 工单待令 |
| L2-1…L2-4（多客户端/能力令牌全量/跨源冲突/运营者章程） | 未启动（设计在案） |
| C6 人话验收 + `sys-v1.0` tag | 治理位动作 |
| 漂移修订 10 条（03 件） | 待令 |

## 八 · 复算命令

```bash
cd D:/zcode-workspace-audit
sed -n '20,50p' ARCHITECTURE-定版v1.1-20260923.md          # 两轴+产品名映射表
py -X utf8 build/assemble.py all                            # 4/4 产物（写 build/out/，可重建）
py -X utf8 build/assemble.py reconcile                      # 五查 PASS
py -X utf8 audit-kit/conformance.py --fused evo-seat/evo_seat.py
```

---
*审计区 · 2026-09-23 · 本图=定版v1.1 的落地实况快照；一处已知图外项=S8 治理操作面未入架构文档（03 件 DR-2）*
