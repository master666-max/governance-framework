# 架构定版 v1.0（2026-09-23 · 冻结）

> **性质**：本体系全部概念设计的**唯一权威综合件**。自本件起，架构进入
> "冻结—交付"模式：**新增概念走修宪程序（§7），交付按 §9 序列一步一步走**。
> 发布口径：仓库内为 docs 层；对外发布时随包分发（与 SPEC 同批）。

---

## §1 一页体系图

```
                    ┌────────── 治理位（人+流程+裁决 · 永在库外 · 不入任何形态）
                    │  签 cap · 仲裁冲突 · 审来源视图 · 修宪
                    ▼
  ┌──────────────── 规范族（§4 · 五件 · 变更走修宪）─────────────────┐
  │  SPEC-内核接口与宿主契约 v1 · AgentAdapter v1 · 四种落地形式 ·   │
  │  库等级体系 · 技术文档-演化机制 v1                              │
  └──────────────────────────┬──────────────────────────────────────┘
                             │ 实现（一码四包 · §3）
  ┌──────────────────────────▼──────────────────────────────────────┐
  │  evocore（演化核：检索·生命周期·决策）  audit-kit（治理核：    │
  │                                        账本·能力·锚·扫描·金标）  │
  └──────┬──────────────┬──────────────┬───────────────┬───────────┘
         │ 装配1        │ 装配2        │ 装配3         │ 装配4
   ┌─────▼─────┐  ┌─────▼─────┐  ┌─────▼─────┐  ┌──────▼──────┐
   │ 微内核     │  │ 中库       │  │ L1 多库    │  │ 共库        │
   │ evo-seat   │  │ (L1.5)    │  │ (v4.3双包) │  │ (L2)        │
   │ 文件里的库 │  │ 包里的库   │  │ pip 依赖   │  │ 进程里的库   │
   └───────────┘  └───────────┘  └───────────┘  └─────────────┘
        L1 单宿主     中库=大库小号同门    L1 多宿主      L2 多宿主共库(MCP)
```

## §2 四档库（等级定义）

| 档 | 名 | 形态一句话 | 写者数 | 治理落地 | 能力令牌 | 状态 |
|---|---|---|---|---|---|---|
| **L1-a** | 微内核 | **文件里的库**（单文件融合） | 1 | 融合段 | ✗（档位替代） | ✅ evo-seat v0.2.1 |
| **L1.5** | **中库** | **包里的库**（skill 内自足） | 1 | 可插拔 gov/ | ⚪ 看 gov 档 | 🔨 规范待建 |
| **L1-b** | L1 多库 | pip 依赖（治理独立包） | 1（同人） | 独立包 | ✅ | ✅ v4.3 在案 |
| **L2** | 共库 | **进程里的库**（MCP 中央） | N（多源） | 服务核 | ✅（刚需复活） | 🔨 设计在案 |

**等级本质差异=信任边界**：L1="自己 vs AI"（C1）→ L2="自己 vs 自己的 Agent
vs 别人的 Agent"（C2 完整形态）→ L3 跨组织（插座）。
**中库定位**（§四形态件 §七）：大库的小号同门——**规模降/部署降/纪律绝不降**；
升格=events 直迁（数据零转换）；vendored 必带 framework_sha 对账。

## §3 一码四包（实现定义）

同一源（evocore + audit-kit）→ 四种装配：**嵌入段**（微内核）/ **vendored
件**（中库 gov/evo/）/ **独立包**（L1 多库）/ **服务核**（L2 server）。
**SPEC §D 五硬条保证同语义**（append-only 物理/链 v2/墓碑/fail-closed/决策
留痕——conformance 可验）——装配方式与规模是变量，**语义不是**。

## §4 规范族（五件 · 冻结索引）

| 件 | 定位 | 变更 |
|---|---|---|
| SPEC-内核接口与宿主契约 v1 | §A 治理面/§B 宿主契约/§C 内核接口/§D 五硬条/§E conformance/§F 对账/§G 注入面 | 修宪 |
| 规范草案-AgentAdapter v1 | 注入/捕获/§六 多 agent 多段/§七 运行注入三档 | 入 SPEC v1.1 待批 |
| 设计-四种落地形式 | 形态矩阵/治理四态/共库落地物/中库落地/一码四包/中库定位 | 修宪 |
| 设计-多 Agent 宿主共库 L2 | 三等级/中央共库裁定/治理七项/§九 微内核不掺和 | 修宪 |
| 技术文档-演化机制 v1 | 机制规格书（哈希 v2/触发器/五参数/生命周期/决策） | 随实现升版 |

## §5 冻结清单（v1.0 冻结 · 改动走修宪）

1. 四档库定义与等级边界（§2）；
2. 一码四包装配原则（§3）；
3. SPEC §D 五硬条（跨形态语义）；
4. 治理位在库外（不问等级不问形态）；
5. 中库三轴裁定：规模降/部署降/**纪律不降**；
6. 微内核不掺和 L2（独立性是功能）；
7. 形态对应关系（哪档用哪种装配）。

## §6 自由清单（实现域 · 各形态自决）

打分公式/向量方案/衰减参数（类型化常量）/CAS 布局/MCP 传输细节/各 Scope
实现/装配器实现/迁移工具实现——**改这些不动架构**。

## §7 变更程序（修宪）

新增概念/改冻结项 = **新版本件**（v1.1/v2.0）+判据先于实现（预注册）+
治理入账（`kind='governance_change'`）——**本件永不被编辑，只被新版本取代**
（只增不改，本体系第一纪律应用于自身架构）。

## §8 血脉（历史锚 · 一句话各线）

三引擎（混元=推演/豆包=压力测试/衔尾蛇=真引擎沙盒）→ bootstrap 引擎族 →
memsys/audit-kit 重写 → evo-seat 融合 → 本定版四档。
**每处设计的实验出处**见三份深度研究件与三份提炼件（290 份报告全读在案）。

## §9 交付序列（一步一个脚印 · 每步可验收）

| 步 | 交付物 | 验收标准 | 依赖 | 预计 |
|---|---|---|---|---|
| **S1** | **evocore 包**（E0：从 memsys/engine 平移三件+tunables，memsys 改引） | 平移零语义变更：双仓测试全绿+行为零差异（对拍） | 无 | 半天 |
| **S2** | **四包装配器**（build 脚本：从一码装配出微内核段/vendored 件/独立包/服务核；产物哈希登记） | 四装配产物全部过 conformance；产物哈希与源可对账 | S1 | 一天 |
| **S3** | **中库规范件**（kb/ 结构模板+manifest schema+migration 工具 embedded→gov/）+conformance 加中库形态检查 | 模板起一个真库跑通；migration 前后数据零变化 | S2 | 一天 |
| **S4** | **L2-0 server 件**（MCP 八工具映射+stdio/HTTP+caps/quota 骨架） | MCP 客户端（任一平台）连上并跑通八工具中至少 append/retrieve/verify | S2 | 一~两天 |
| **S5** | **端到端演示**：三形态各跑一遍+**升格实测**（中库→共库 events 直迁） | 演示记录在案：微内核自检/中库建库检索/共库双客户端并写/升格直迁后链完整 | S3+S4 | 一天 |
| **S6** | **文档收束**：三形态说明书+迁移指南（升格/降格/装配切换） | 零代号读者可照做（人话验收） | S5 | 一天 |
| **S7** | **交付评审**：conformance 全形态+文档+演示齐套+发布件打包 | 评审过→打 tag（体系 v1.0） | S6 | 半天 |

**并行轨**（不阻塞主序）：
- T-1 可吸收经验批一批二（文本+SOP+evo-seat 小改）；
- T-2 门线 F1-F4 修复（承接线发版）；
- T-3 钩子向各田部署；
- T-4 每日档/观察项点检（09-29 后）。

## §10 边界

- 本件是**架构定版**不是实施令——S1 起每步开工前走各自流程（预注册/验收）；
- 概念讨论自此收敛：**再有新想法=记入修宪候选清单**（不打断交付）；
- 三线未读面/登记不吸收项（席位族/演化栈等）维持原判。

---
*审计区 · 2026-09-23 · **架构定版 v1.0（冻结）** · 交付序列 S1-S7 待令开工*
