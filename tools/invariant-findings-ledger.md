# invariant-findings-ledger.md — 扫描器命中处置台账（20260919 N-9 缺口闭合件）

> 缺口来源：补遗二 N-9（收编副本在案）——「R1 的 8 个候选每个都有归宿，R2 的
> 1 个命中没有归宿：检出面在响，处置台账里没有它」。本台账即处置面。
> 维护纪律：**任何 R1/R2/R1-kw 命中必须在交付/转发前于本台账登记行**（检出≠处置）；
> 处置态=待路由/路由中/已处置（附归宿锚）/豁免（附证据）。

## 一、R1 命中处置（8 条，处置权=混元田，台账镜像）

处置详见主区 `混元/kernel主线/invariants-处置与常数簿-20260919.md`@24d8075
（8/8 逐条三态：真死 2 登记 v894=degrade_thr 配置残留+task_seed 元层残留；
豁免 5=序列化面/输入面/跨载体；K-A 本人 Policy.deep=机械检出成立登记 v894）。
台账镜像（claim 摘要）：budget_spent 豁免(序列化面) / **deep 真·K-A→v894** /
degrade_thr 真死→v894 / importance 豁免(输入面) / notes 豁免(kw 注入,v1.0 漏报
已修 v1.1) / policy 豁免(快照面) / task_seed 真死(元层)→v894 / topic 豁免(跨载体)。

## 二、R2 命中处置（1 条，本台账立此存照）

| # | 命中 | 检出源 | 处置态 |
|---|---|---|---|
| R2-1 | `混元/kernel主线/验收-v893/verify_kernel_v2.py@5324130bc7fc:326` —— `check("reanchors 字段存在且与 reverts 分开", hasattr(kr,"reanchors"))` 恒真（reanchors 为 `kernel.py:445` 无条件赋值属性，条件不可失败）；名实不符（reverts 系 Report 字段 :407/:900，reanchors 系 Kernel 属性 :901，类型上本就不同物）；后果=主门归档 32 条 PASS 中 1 条（:67，3.125%）由不可失败条件注入 | 审计区条目 091 扫描器自测（R2a'）+补遗二 §2.4 独立定谳 | **已处置（bbe1a21，2026-09-19）**：恒真 hasattr 改**可失败判据**（两计数器独立性=reverts 聚合完整性，逐代和==final_report.reverts 互斥递进语义，verify_kernel_v2.py:326-329 注记+:131/:300 实现）；主门完整回归 32/0+DX 11 判定行 exit=0（归档 v893_main_gate_r2.log 85 行）；处置期间自纠一次半恒真草稿（如实登记）——**台账闭合，pre-commit R2 闸门自此对该文件生效** |

## 三、工具改进登记（扫描器本体）

| 项 | 来源 | 状态 |
|---|---|---|
| R1 补构造器 kw 识别（kw 注入型≠死字段） | 混元处置件反馈（Report.notes 漏报） | **v1.1 已修**（20260919，自测与混元人读三态逐条收敛：R1-kw 5+R1 3） |
| R2 预防性闸门（新增 check() 不得静默增加不可证伪面） | 补遗二 N-9（处方部分已由知会件撤销，此处仅登记方向） | **已采纳并上线（条目 093，用户令"增加"；pre-commit 检查 4，钩子 v2 sha `e1a8f070` 双处一致）**——20260919 回执卡差额3 对账更正：本行原"未采纳（登记）"系条目 092 建件时的旧态，闸门于 093 shipped 后本行未随更新（三向不一致，以工件为准） |
| 链式死可达性分析（R1+读取者存活分析，治 verified_against/stale 型） | 评估卷⑱ | v2 候选（登记） |

## 四、引用点带基准规则（N-11 本区先例）

跨件引用外部计数必须就地携带三元组（源/基准/域），两基准并存时并列给值。
本区先例：constants-registry.md 格式；反例在案=本区下发单 `:41`「172 条 assert」
附 blob 未附匹配模式（基准部分脱落——公理二不改写原件，本行即更正记录：
正确引用=`172 条 assert（源: 补遗单@6a08d007 §三 · 基准: 正则 (^|[^_.a-zA-Z])assert · 域: bootstrap_v3.py@21e88e0e13e6）；168（独立语句行首口径）`。

## 四、R2 命中处置（audit-kit 批，2026-09-23）

| # | 命中 | 检出源 | 处置态 |
|---|---|---|---|
| R2-2 | `audit-kit/tests/test_matrix_ak.py:152` —— `check(stand_in, "delete", …)` 字面恒真 | audit-kit W0-W2 质量门批（条目 170，R2 预防闸门） | **豁免（纵深防御）**：鸭子类型 stand-in（SimpleNamespace 绕构造校验）专测第四关 DENY:CANNOT 的先后顺序；真实 Capability 因构造器 can∩cannot=∅ 使第三关恒假，该分支对真实对象不可达——check() 第三关保留为纵深防御。复评条件=check() 四关顺序重构或构造校验放宽 |
| R2-3 | `audit-kit/tests/test_matrix_ak.py:255` —— `check(holder, "append_events", …)` 字面恒真 | 同上 | **豁免（纵深防御）**：holder 由签发 payload 重建（可经构造校验的属性子集），用例守 check() 内部逻辑顺序；同 R2-2 复评条件 |
