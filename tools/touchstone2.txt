# -*- coding: utf-8 -*-
"""touchstone2.txt — 试金石#2：猴子记忆库推演/自演化推演_实验总报告17轮后.md
（879行·长而复杂）。experiment_id 用宇宙前缀 SIM-E{n}（离线推演宇宙，
与真引擎宇宙 REAL-* 区分；正式规范化在阶段1 alias_map）。
source_type=document（结构化实验报告，非会话导出；作者归属问题留阶段1）。"""
import os

AUD = r"D:\zcode专用！！！！危险！！！！！！！！！\workspace-audit"
ns = {}
exec(compile(open(os.path.join(AUD, "tools", "ledger_tool.txt"), encoding="utf-8").read(), "ledger_tool", "exec"), ns)
ledger_add = ns["ledger_add"]

SRC = "猴子记忆库推演/自演化推演_实验总报告17轮后.md"

# (line, quote, claim_type, experiment_id, status)
claims = [
    (3, "> **范围**：从代码审查到十七轮自演化推演的全部实测结果（**80 个实验** + 3 个架构原型 + **19 次实验设计失败记录** + **15 次自我推翻**）", "parameter", "SIM-REPORT", "open"),
    (14, "| **代码层** | 核心三件套（记忆+审计+治理）165 行 = 6.8%；内容分发 213 行 > 核心。P0×4 / P1×8 / P2×13 |", "result", "SIM-REPORT", "open"),
    (15, "| **架构层** | 公理 D（双平面）物理上不成立——审计链不含 payload，系统是四条平行真相。截断攻击完全检不出 |", "verdict", "SIM-REPORT", "open"),
    (17, "| **最深发现** | 系统输出会塑造评判它的人（E14）：自报 +1.50 时真实 −0.83。终极安全机制不是\"防止腐蚀\"，是\"腐蚀时能被看见并停下\" |", "result", "SIM-E14", "open"),
    (20, "| **底线（E40）** | 有漂移时 v3 退化到 **−0.1124（不如不演化）**，修正后架构保持 **+0.1492**。差 0.26 |", "result", "SIM-E40", "open"),
    (40, "1. **自测结论写死**：`cmd_run_tests` 用 `except Exception`，捕获不到 `SystemExit`（BaseException），而全文件 24 处 `sys.exit` 是唯一错误通道。干净检出下跑到第 10 个用例静默中止。", "verdict", "SIM-CODEAUDIT", "open"),
    (98, "系统实际是**四条平行真相**：`audit/`（事件指针）、`ledger/`（变更流水）、`memory/`（内容正本）、`state.json`（派生量快照）。`doctor` 的四方对账不是\"投影 vs 真相\"，是四个真相两两交叉核对。", "verdict", "SIM-CODEAUDIT", "open"),
    (117, "Merkle 根锚定**内容集合**，不锚定**序列长度**。截断后剩余各行仍自洽。`state` 中无 `chain_tip` 字段。", "verdict", "SIM-CODEAUDIT", "open"),
    (174, "| 1 | 自演化有效性 | 留出集 0.033→0.814，8/8 正 | 强 |", "result", "SIM-E1", "open"),
    (180, "| **7** | **两级门控** | 准入严格 0/20；宽松+严格晋升 20/20 | **决定性** |", "result", "SIM-E7", "open"),
    (182, "| 8 | 留出集污染 | 自报 0.094 vs 真实 0.025（3.7×） | 强 |", "result", "SIM-E8", "contradicted"),
    (187, "| **14** | **评判者漂移** | **自报 +0.51，真实 −0.09** | **决定性** |", "result", "SIM-E14", "open"),
    (202, "| **28** | **层次化归并** | **远差于平铺（0.12 vs 0.90）；推翻 E23 建议** | **否定性** |", "result", "SIM-E28", "open"),
    (205, "| 31 | 留出集轮换 | 未复现 E8 膨胀；结论不成立 | 否定性 |", "result", "SIM-E31", "open"),
    (251, "| **80** | **复合灾难** | **v4 反而更差(−0.26 vs −0.11)**：护栏负交互 | **负面** |", "result", "SIM-E80", "open"),
    (331, "**自报的进步是真实的 3.7 倍。** 未失控（150 代后平台化，训练集门起了刹车），但膨胀真实且持续。", "result", "SIM-E8", "contradicted"),
    (457, "**α=0.10 时：系统自报 +1.50，真实 −0.83（从 +0.26 跌到 −0.57）。它在讨好人，不是在变好。而内部所有指标全绿——双门全程通过，感知效用一路上升。**", "result", "SIM-E14", "open"),
    (459, "> ⚠️ **数据修正**：本表为第五轮修复特征缓存 bug 后的复核值。原报告（α=0.20 真实 0.6906 / 起点 0.7844）受 `id()` 复用导致的缓存串味影响。**结论方向不变，且实际比原报告严重得多**。E16 已复核确认不受影响（0.5469 / 0.7188）。", "method", "SIM-E14", "open"),
    (522, "1. 固定 margin 假阳性 **0.42**（n=10）——小样本下近一半\"改进\"是纯噪声。这是 E5 与 E13 的共同机制", "result", "SIM-E17", "open"),
    (547, "| 5 | `expand_links` 是死代码 | 链接到 `f{i}` 而世界里只有 `e{i}`，**该基因从未生效** | 前三轮凡涉及它的结论都需打折 |", "verdict", "SIM-FAILLOG", "open"),
    (581, "**方向性约束**：变更只能向上申请，不能向下修改。法层永远改不了元层——**系统无法修改评判自己的尺子**。", "method", "SIM-ARCH", "open"),
    (630, "13. **任何影响\"是否被采纳\"的参数必须冻结，或只能朝更严格方向演化**（E24 修正 E10）", "method", "SIM-E24", "open"),
    (661, "**有漂移时 v3 退化到 −0.1124——比它自己的起点还差，即\"跑完一整套演化，产物不如不演化\"，而内部指标全绿。**", "result", "SIM-E40", "open"),
    (704, "**0.90 → 0.85**。第四轮的\"8/8 检出\"是在一个几乎永远报警的设置下取得的，含金量很低。", "result", "SIM-E34", "open"),
    (764, "> 有效的方案全是\"测量/利用已有可观测量\"，无效的 11 条全是\"发明新启发式\"。", "hypothesis", "SIM-REPORT", "open"),
    (797, "> 已确认：本架构在 **≥3 种危险同时出现时不可靠**。这是必须写入文档的边界。", "verdict", "SIM-E80", "open"),
    (827, "1. **让漂移可见**（锚定集审计，阈值 **0.85**，零误报且 5 代内检出）", "method", "SIM-E14", "open"),
    (835, "**自演化可行且值得**（E40：无漂移 +0.044，有漂移 +0.262），但**必须配全套护栏**——不是护栏让它变好，是没护栏时它会自信地走错且指标全绿。", "verdict", "SIM-E40", "open"),
    (843, "终极安全机制不是\"防止被腐蚀\"，而是\"**腐蚀发生时能被看见并停下来**\"。", "hypothesis", "SIM-E14", "open"),
]

for line, quote, ctype, eid, status in claims:
    cid, res = ledger_add(SRC, line, quote, ctype, eid, "document", status)
    print(f"{res:10s} {cid[:16]}  :{line}  [{ctype}/{eid}]")

print("--- 幂等复跑 ---")
dup_ok = all(ledger_add(SRC, l, q, t, e, "document", s)[1] == "duplicate" for l, q, t, e, s in claims)
print("幂等性:", "PASS" if dup_ok else "FAIL")
