# 治理体型 · 模块详解（关系全部取自本轮实测测绘，非印象）

## 〇 · 一张图先立骨架

```
                    ┌───────────── 治理位（人 · 库外，任何目录里都没有它）─────────────┐
                    │   签凭证 · 仲裁冲突 · 验收裁定 · 修宪 —— 仓里只有它的笔迹(LOG/裁定索引) │
                    └──────────────────────────┬──────────────────────────┘
                                               │ 验收与令
   ┌─────────────── 规范层（docs/：定版·SPEC·裁定索引·LOG）───────────────┐
   │                                                                     │
   │            ┌──── 演化核（evocore）────┐      ┌──── 治理核（audit-kit）────┐ │
   │            │ 检索·生命周期·决策·指纹 │◄─────│ 账本·凭证·预注册·符合性  │ │
   │            └───────────┬───────────┘ 指纹  └───────────┬──────────────┘ │
   │                        │      ┌──── 记忆库（memsys）────┘               │
   │                        └──────│ 桥接件（bridge）＝双核的活体装配示例      │
   │                               └────────────┬───────────────────────────┘
   │                                            │ 源码
   │                     ┌──── 装配器（build，assemble.py）────┘
   │                     │ 四种产物：微内核(evo-seat) / 中库(kb) / 独立包(pypkg) / 共库(server)
   │                     └────────────┬─────────────────────────────────────
   └──────────────────────────────────┼─────────────────────────────────────
        门与对账 tools/ ──────────────┘（治理门·质量门·对拍器·副本闸——检验上面所有人）
        账目管线 imap/imap2 ──────────┐（主权账目 claim_ledger.csv 的写入与分析链）
        物证簇 data/red-team/audit ───┘（只读封存，实验读数的出处）
```

## 一 · 双核（体系的地基，互不引用——这是设计不是巧合）

**演化核（`evocore/`，14 件）**：管"**记忆怎么活着**"。内容指纹（`entry.py` 的
`content_hash`）是全体系唯一指纹来源（64 位十六进制，排除 id/state 等易变字段——规则钉在
装配器测试 `test_content_hash_divergence.py`）；生命周期状态机（`lifecycle.py`：
活跃→阁下→墓碑，只迁移不销毁）；检索打分（`retrieval.py`）；决策规则（`decision.py`）；
参数全部外置（`tunables`，版本可协商）。**零第三方依赖，纯标准库**。

**治理核（`audit-kit/`，14 件）**：管"**谁被允许做什么、做了必留痕**"。链式只增账本
（`ledger/ledger.py`：即写即事务、触发器物理禁改禁删、开库即验链）；能力令牌
（`core/gov_types.py` 的 `Capability`：准许∩禁止，构造即拒）；预注册判据
（`core/prereg.py`）；自举件（`boot_self.py`：用自己的账本记自己的提交）。
**实测中记忆库和装配器都依赖它的凭证与账本**——它是被依赖最广的核。

**关系**：演化核不知道治理核存在，治理核不引用演化核。唯一交点是那个指纹函数——
记忆内容的完整性由治理侧可验证，但两侧代码零耦合。这就是"执行并集、证据不同根"的
元层公共解。

## 二 · 记忆库（memsys）——当前的宿主实例（7 件）

桥接件（`bridge.py`）是全仓最关键的**关系件**：它同时引用治理核的凭证与账本、演化核的
内容指纹，把两核粘成一个可用的记忆库。它证明了"双核可装配"不是 PPT——**改动任一核，
桥接件和它的 17 个测试立刻知道**。类型注册表（`kinds.yml`）与桥接件内置表的对拍由双包
对拍器（`tools/tools_crosscheck.py`）每次执行（本轮刚把对账器迁进门面，目录环清零）。

**宿主的本质（这节最重要）**：记忆库不是体系本身，它只是**当前接入的宿主实例**——第一个
装配示例。宿主＝被治理的执行体：真正持有内容、动手写入的那一层。治理双核对宿主一无所
知，只通过四个接触点相连：①写前出示凭证；②内容过指纹；③事件进账本；④状态可对拍。
⇒ 任何能满足这四个接触点的系统（LLM wiki / 工件库 / 数据库 / 工作流审计 / 任意智能体的
知识仓）都能当宿主；换宿主，双核零改动、门面零改动，治理强制力原样生效。治理位在库外、
不寄生于宿主内部——**通用性与可替换性不是口号，是四个接触点的几何结果**。

## 三 · 装配器（build，一码四包，34 件）

装配器（`assemble.py`）从**同一份双核源码**机械产出四种形态：微内核（单文件融合，即
evo-seat 的生成法）、中库（自足包，kb 模板）、独立包（pip）、共库（服务件）。共库服务件
（`l2server/server.py`）是 L2 形态的多宿主共库（发卡即定身份已实现）；测试面（`tests/`，
131 测）是**全体系最重的裁判**（裁判有效性实测：演化核的性质出了事，是装配器的门先红）。
**关系红线**（测绘最危险项）：装配器按路径+字符串模板镜像源码——**动双核文件必须同步
装配器，否则发布树静默漂移**。发布清单（`manifests/`，四份）登记产物哈希，是发布对账
的锚。

## 四 · 门与对账面（tools，94 件——体系的免疫系统）

不在生产数据流里，专门**检验别人**：治理门（`hooks/pre-commit`：账本只追加/日志撞号/
副本一致性/判据面四查，仓库副本在 `tools/hooks/`）；判据扫描器（`invariant_scanner.py`）；
五查质量门（`code_quality_gate.py`）；行为对拍器（`S1_verify_behavior.py`，新旧引擎
33 用例）；双包对拍器（`tools_crosscheck.py`，本轮迁入）；副本族闸
（`check_vendored_copies.py`，本轮新建）。**与演化核的关系是单向消费者**（基准件和行为
对拍器引用它做验证）。

## 五 · 账目管线（imap 146 件 / imap2 357 件）

两代"主权账目"管线：账目写入器（`imap/tools/append_ledger.py` 与
`imap2/tools/append2.py`）是仓根主权账目 `claim_ledger.csv`（只增，治理门第一查物理
保护）的唯二入口——上一轮刚给它们加固了锁与时间戳旁账，本轮定活时因此豁免删除。其余
扫描/合并/物化是分析链（本轮确认全死，已删 26 件）。imap2 是 imap 的**重写**（相似度
仅 0.64，非复制），保留是"冻结件不改"惯例。

## 六 · 发布面（skill 23 件 + sekb-plugin / sekb-dsh-package / tools/skill-installer）

把体系装进"能发给别人"的壳：技能主页（`SKILL.md`）+ 迁移器（`migrate.py`，四份字节级
副本，副本族闸的上闸对象）+ 判据扫描器旧谱系（与门面现役版的分叉是已知挂账）。分发单元
**故意自足**（自带副本不是病，漂移才是——所以闸的是一致性不是重复）。

## 七 · 物证与报告（data 48 / red-team 162 / audit / docs 47 / 交接包）

红队区（`red-team/`）、沙盒簇（`audit/`，v3/v4 族）、数据区（`data/`）是**实验读数的
物证**——结论可弃、证据不可弃，整簇封存豁免。报告层（`docs/`）：架构定版、SPEC、S1-S8
交付、缺陷账。交接包（`交接包_20260922/`）是历史交接快照。外线镜像
（`_research-hub-sync/`）**未跟踪、零写入**——所以它没上 GitHub。

## 八 · 一次真实写入的数据流（把上面串起来）

宿主智能体调记忆库的记账器写一条记忆 → 先向治理核要凭证（无凭证即拒）→ 演化核算内容
指纹（去重与完整性锚）→ 治理核账本把事件追加进链（含前哈希衔接）→ 写毕，双包对拍器可
对拍双库同链，治理门保证没人能删账，远端的完整历史就是这条纪律走到今天的全部脚印。

一句话收束：**演化核管记忆的生命，治理核管行为的账，记忆库证明两者能装在一起，装配器
证明能装成四种形态，门与对账面负责怀疑一切，物证簇负责记得为什么，治理位（你）负责
拍板**。
