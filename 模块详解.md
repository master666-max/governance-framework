# 治理体型 · 模块详解（关系全部取自本轮实测测绘，非印象）

## 〇 · 一张图先立骨架

```
                    ┌───────────── 治理位（人 · 库外，任何目录里都没有它）─────────────┐
                    │   签凭证 · 仲裁冲突 · 验收裁定 · 修宪 —— 仓里只有它的笔迹(LOG/裁定索引) │
                    └──────────────────────────┬──────────────────────────┘
                                               │ 验收与令
   ┌─────────────── 规范层（docs/：定版·SPEC·裁定索引·LOG）───────────────┐
   │                                                                     │
   │            ┌──── 演化核 evocore ────┐      ┌──── 治理核 audit-kit ────┐ │
   │            │ 检索·生命周期·决策·指纹 │◄─────│ 账本·凭证·预注册·符合性  │ │
   │            └───────────┬───────────┘ 指纹  └───────────┬──────────────┘ │
   │                        │      ┌──── 记忆库 memsys ────┘                │
   │                        └──────│ bridge = 双核的活体装配示例             │
   │                               └────────────┬───────────────────────────┘
   │                                            │ 源码
   │                     ┌──── 装配器 build（assemble.py）────┘
   │                     │ 四种产物：微内核(evo-seat) / 中库(kb) / 独立包(pypkg) / 共库(server)
   │                     └────────────┬─────────────────────────────────────
   └──────────────────────────────────┼──────────────────────────────────
        门与对账 tools/ ──────────────┘（治理门·质量门·对拍器·副本闸——检验上面所有人）
        账目管线 imap/imap2 ──────────┐（claim_ledger.csv 的写入与分析链）
        物证簇 data/red-team/audit ───┘（只读封存，实验读数的出处）
```

## 一 · 双核（体系的地基，互不 import——这是设计不是巧合）

**`evocore/`（演化核，14 件）**：管"**记忆怎么活着**"。`entry.py` 的 `content_hash` 是全体系唯一指纹来源（64 位 hex，排除 id/state 等易变字段——规则钉在 build 仓的 `test_content_hash_divergence.py`）；`lifecycle.py` 状态机（ACTIVE→ATTIC→TOMBSTONE，只迁移不销毁）；`retrieval.py` 检索打分；`decision.py` 决策规则；`tunables` 参数全部外置（版本可协商）。**零第三方依赖，纯标准库**。

**`audit-kit/`（治理核，14 件）**：管"**谁被允许做什么、做了必留痕**"。`ledger/ledger.py` 是 append-only 链账本（BEGIN IMMEDIATE 事务化、触发器物理禁 UPDATE/DELETE、开库即验链）；`core/gov_types.py` 是能力令牌 `Capability`（can∩cannot 构造即拒）；`core/prereg.py` 预注册判据；`boot_self.py` 用自己的账本记自己的提交。**实测中 memsys 和 build 都依赖它的 gov_types + ledger**——它是被依赖最广的核。

两者关系：**evocore 不知道 audit-kit 存在，audit-kit 不 import evocore**。唯一交点是那个指纹函数——记忆内容的完整性由治理侧可验证，但两侧代码零耦合。这就是"执行并集、证据不同根"的元层公共解。

## 二 · memsys（双核的活体装配示例，7 件）

`bridge.py` 是全仓最关键的**关系件**：它同时 import `audit-kit/core`（Capability、Ledger）和 `evocore.entry`（content_hash），把两核粘成一个可用的记忆库。它证明了"双核可装配"不是 PPT——**改动任一核，bridge 和它的 17 个测试立刻知道**。`kinds.yml` 声明记忆类型注册表，与 bridge.KINDS 的对拍由 `tools/tools_crosscheck.py` 每次执行（本轮刚把对账器迁进 tools/，目录环清零）。

## 三 · build（装配器：一码四包，34 件）

`assemble.py` 从**同一份双核源码**机械产出四种形态：`fused`（单文件微内核，即 evo-seat 的生成法）、`vendored`（中库自足包，kb 模板）、`pypkg`（pip 独立包）、`server`（L2 共库）。`l2server/server.py` 是 L2 形态的 MCP 服务件（多宿主共库，B′ 发卡身份已实现）；`tests/` 131 个测试是**全体系最重的裁判**（D 步实测：evocore 的性质出了事，是 build 的门先红）。**关系红线**（MAP 最危险项）：assemble 按路径+字符串模板镜像源码——**动双核文件必须同步装配器，否则发布树静默漂移**。`manifests/` 四份清单登记产物哈希，是发布对账的锚。

## 四 · tools（门与对账面，94 件——体系的免疫系统）

不在生产数据流里，专门**检验别人**：`hooks/pre-commit`（治理门：append-only/LOG 撞号/SOP 副本/R2 不可证伪面四查，仓库副本在 `tools/hooks/`）；`invariant_scanner.py`（R2 判据）；`code_quality_gate.py`（五查质量门，圈复杂度 Top 担当）；`S1_verify_behavior.py`（新旧引擎行为对拍 33 用例）；`tools_crosscheck.py`（audit-kit⇄memsys 双包对拍，本轮迁入）；`check_vendored_copies.py`（本轮新建，副本族逐字节闸）。**与 evocore 的关系是单向消费者**（bench 和 S1 导入它做验证）。

## 五 · 账目管线（imap 146 件 / imap2 357 件）

两代"主权账目"管线：`imap/tools/append_ledger.py` 与 `imap2/tools/append2.py` 是仓根 **`claim_ledger.csv`（append-only，pre-commit 检查1 物理保护）的唯二写入器**——上轮 P1 刚给它们加固了锁与时间戳 sidecar，本轮定活时因此豁免删除。其余 scan/merge/materialize 是分析链（本轮确认全死，已删 26 件）。imap2 是 imap 的**重写**（相似度仅 0.64，非复制），保留是"冻结件不改"惯例。

## 六 · 发布面（skill 23 件 + sekb-plugin / sekb-dsh-package / tools/skill-installer）

把体系装进"能发给别人"的壳：`SKILL.md` + `migrate.py`（四份字节级副本，`check_vendored_copies.py` 的上闸对象）+ `invariant_scanner.py` 旧谱系（与 tools/ 现役版的分叉是已知挂账）。分发单元**故意自足**（vendored 副本不是病，漂移才是——所以闸的是一致性不是重复）。

## 七 · 物证与报告（data 48 / red-team 162 / audit / docs 47 / 交接包）

`red-team/`、`audit/`（沙盒 v3/v4 族）、`data/` 是**实验读数的物证**——结论可弃、证据不可弃，整簇封存豁免。`docs/` 是报告层：架构定版、SPEC、S1-S8 交付、`02-bugs`。`交接包_20260922/` 是历史交接快照。`_research-hub-sync/` 是**外线镜像，未跟踪、零写入**——所以它没上 GitHub。

## 八 · 一次真实写入的数据流（把上面串起来）

宿主 agent 调 `memsys.bridge.MemoryLedger` 写一条记忆 → bridge 先问 `audit-kit.gov_types` 要凭证（无凭证即拒）→ `evocore.entry.content_hash` 算内容指纹（去重与完整性锚）→ `audit-kit.ledger` 以 BEGIN IMMEDIATE 把事件追加进链（含 prev_hash 衔接）→ 写毕，`tools_crosscheck` 可对拍双库同链，`pre-commit` 保证没人能删账，远端 GitHub 上 `7c84ae5` 的完整历史就是这条纪律走到今天的全部脚印。

一句话收束：**evocore 管记忆的生命，audit-kit 管行为的账，memsys 证明两者能装在一起，build 证明能装成四种形态，tools 负责怀疑一切，物证簇负责记得为什么，治理位（你）负责拍板**。
